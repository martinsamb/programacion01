#include <stdio.h>
#include <stdlib.h>

int main()
{
    char a;
    char *P;
    int b=2;
    int *pEntero;

    //aritmeticas de punteros
    printf("->La variable a es \n",a);
    printf("->*p es %p\n",*p);
    printf("->p es %p\n",p);

    //incrementar
    p = p+2;
    printf("\n->Sumo 2 -> p ahora es %p\n",p);
    printf("\n->Contenido de la variable a la que apunta p %c\n")

    return 0;
}
